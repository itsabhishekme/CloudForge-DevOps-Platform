apiVersion: v1
kind: Service

metadata:
  name: cloudforge-backend
  namespace: cloudforge

  labels:
    app: cloudforge-backend
    component: backend

spec:
  type: ClusterIP

  selector:
    app: cloudforge-backend

  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000