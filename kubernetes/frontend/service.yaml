apiVersion: v1
kind: Service

metadata:
  name: cloudforge-frontend
  namespace: cloudforge

  labels:
    app: cloudforge-frontend
    component: frontend

spec:
  type: ClusterIP

  selector:
    app: cloudforge-frontend

  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000