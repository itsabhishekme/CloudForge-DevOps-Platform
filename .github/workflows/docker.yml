name: Kubernetes Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configure kubectl
        run: |
          echo "Configure Kubernetes credentials here"

      - name: Deploy
        run: |
          kubectl apply -f kubernetes/